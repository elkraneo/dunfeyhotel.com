# Apple-sourced data is never committed here: it lives in the private data
# repo, mounted as the `data` submodule (relative URL, so Coolify's GitHub App
# token and your own credentials both resolve it). No access? Provision data/
# yourself — see README.

node_modules/
dist/
.astro/
.DS_Store
